2026-06-02 — fixed duplicate-screenshot bug: _kill_port was a no-op on Railway (no fuser), leaving zombie web servers that every later run screenshotted; now uses psutil, test server is killed after verify, and screenshotter injects PORT
